# ===================================================================================
# GOVCONNECT - CI/CD PIPELINE (CLEAN VERSION)
# ===================================================================================

name: CI/CD Pipeline

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      service:
        description: 'Service to deploy'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - dashboard
          - channel-service
          - ai-service
          - case-service
          - notification-service

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository_owner }}/govconnect

jobs:
  # ===================================================================================
  # BUILD DASHBOARD
  # ===================================================================================
  build-dashboard:
    name: Build Dashboard
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./govconnect-dashboard
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-dashboard:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-dashboard:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_APP_URL=https://govconnect.my.id

  # ===================================================================================
  # BUILD CHANNEL SERVICE
  # ===================================================================================
  build-channel:
    name: Build Channel Service
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./govconnect-channel-service
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-channel-service:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-channel-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ===================================================================================
  # BUILD AI SERVICE
  # ===================================================================================
  build-ai:
    name: Build AI Service
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./govconnect-ai-service
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-ai-service:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-ai-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ===================================================================================
  # BUILD CASE SERVICE
  # ===================================================================================
  build-case:
    name: Build Case Service
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./govconnect-case-service
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-case-service:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-case-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ===================================================================================
  # BUILD NOTIFICATION SERVICE
  # ===================================================================================
  build-notification:
    name: Build Notification Service
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./govconnect-notification-service
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-notification-service:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-notification-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ===================================================================================
  # DEPLOY TO VPS
  # ===================================================================================
  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest
    needs: [build-dashboard, build-channel, build-ai, build-case, build-notification]
    if: |
      always() && 
      github.ref == 'refs/heads/main' &&
      needs.build-dashboard.result == 'success'
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Validate required secrets
        run: |
          echo "🔍 Validating required secrets..."
          
          if [ -z "${{ secrets.VPS_HOST }}" ]; then
            echo "❌ VPS_HOST secret is not set"
            exit 1
          fi
          
          if [ -z "${{ secrets.VPS_USER }}" ]; then
            echo "❌ VPS_USER secret is not set"
            exit 1
          fi
          
          if [ -z "${{ secrets.VPS_SSH_KEY }}" ]; then
            echo "❌ VPS_SSH_KEY secret is not set"
            exit 1
          fi
          
          if [ -z "${{ secrets.GHCR_TOKEN }}" ]; then
            echo "❌ GHCR_TOKEN secret is not set"
            exit 1
          fi
          
          echo "✅ All required secrets are set"

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Test SSH connection
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
        run: |
          echo "🔗 Testing SSH connection..."
          ssh -o ConnectTimeout=10 $VPS_USER@$VPS_HOST "echo 'SSH connection successful'" || {
            echo "❌ SSH connection failed"
            exit 1
          }

      - name: Update ENV file on server
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          ENV_FILE: ${{ secrets.ENV_FILE }}
        run: |
          if [ -n "$ENV_FILE" ]; then
            echo "📝 Updating .env file on server..."
            ssh $VPS_USER@$VPS_HOST "mkdir -p ~/genfity/govconnect"
            echo "$ENV_FILE" | ssh $VPS_USER@$VPS_HOST "cat > ~/genfity/govconnect/.env"
            echo "✅ ENV file updated!"
          else
            echo "⏭️ ENV_FILE secret not set, skipping .env update"
          fi

      - name: Deploy with Docker Compose
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
        run: |
          echo "🚀 Deploying GovConnect to VPS..."
          
          ssh $VPS_USER@$VPS_HOST << ENDSSH
            set -e
            cd ~/genfity/govconnect
            
            # Force pull latest changes (override local, no conflicts)
            git fetch origin main
            git reset --hard origin/main
            
            # Login to GHCR using PAT
            echo "$GHCR_TOKEN" | sudo docker login ghcr.io -u mygads --password-stdin
            
            # Create networks if not exists
            sudo docker network create govconnect-network 2>/dev/null || true
            sudo docker network create infra-network 2>/dev/null || true
            
            # Set image variables (only for successfully built services)
            export IMAGE_DASHBOARD=ghcr.io/mygads/govconnect-dashboard:latest
            
            # Try to set other images if they exist
            if docker manifest inspect ghcr.io/mygads/govconnect-channel-service:latest >/dev/null 2>&1; then
              export IMAGE_CHANNEL=ghcr.io/mygads/govconnect-channel-service:latest
            fi
            
            if docker manifest inspect ghcr.io/mygads/govconnect-ai-service:latest >/dev/null 2>&1; then
              export IMAGE_AI=ghcr.io/mygads/govconnect-ai-service:latest
            fi
            
            if docker manifest inspect ghcr.io/mygads/govconnect-case-service:latest >/dev/null 2>&1; then
              export IMAGE_CASE=ghcr.io/mygads/govconnect-case-service:latest
            fi
            
            if docker manifest inspect ghcr.io/mygads/govconnect-notification-service:latest >/dev/null 2>&1; then
              export IMAGE_NOTIFICATION=ghcr.io/mygads/govconnect-notification-service:latest
            fi
            
            # Pull all images first (parallel) with retry
            echo "📥 Pulling images..."
            for i in {1..3}; do
              sudo -E docker compose pull && break || {
                echo "Pull attempt \$i failed, retrying..."
                sleep 10
              }
            done
            
            # Stop old containers gracefully
            echo "🛑 Stopping old containers..."
            sudo -E docker compose down --remove-orphans || true
            
            # Start services that have images available
            echo "🚀 Starting available services..."
            
            echo "Starting dashboard..."
            sudo -E docker compose up -d dashboard
            sleep 10
            
            # Start other services if their images are available
            if [ -n "$IMAGE_CHANNEL" ]; then
              echo "Starting channel-service..."
              sudo -E docker compose up -d channel-service
              sleep 10
            fi
            
            if [ -n "$IMAGE_CASE" ]; then
              echo "Starting case-service..."
              sudo -E docker compose up -d case-service
              sleep 10
            fi
            
            if [ -n "$IMAGE_NOTIFICATION" ]; then
              echo "Starting notification-service..."
              sudo -E docker compose up -d notification-service
              sleep 10
            fi
            
            if [ -n "$IMAGE_AI" ]; then
              echo "Starting ai-service..."
              sudo -E docker compose up -d ai-service
              sleep 10
            fi
            
            # Check container status
            echo "📊 Container status:"
            sudo docker compose ps
            
            # Cleanup old images
            echo "🧹 Cleaning up..."
            sudo docker image prune -af --filter "until=24h" 2>/dev/null || true
            
            echo "✅ Deployment complete!"
          ENDSSH

      - name: Run Database Migrations
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
        run: |
          echo "🗃️ Running database migrations..."
          ssh $VPS_USER@$VPS_HOST << 'ENDSSH'
            # Wait for containers to be ready
            echo "⏳ Waiting for containers to be ready..."
            sleep 20
            
            # Check if PostgreSQL is accessible
            echo "🔍 Checking PostgreSQL connection..."
            if ! sudo -u postgres psql -c '\l' >/dev/null 2>&1; then
              echo "❌ PostgreSQL not accessible, skipping migrations"
              exit 0
            fi
            
            # Create databases if they don't exist
            echo "📦 Creating databases..."
            sudo -u postgres createdb gc_dashboard 2>/dev/null || echo "Database gc_dashboard already exists"
            sudo -u postgres createdb gc_channel 2>/dev/null || echo "Database gc_channel already exists"
            sudo -u postgres createdb gc_ai 2>/dev/null || echo "Database gc_ai already exists"
            sudo -u postgres createdb gc_case 2>/dev/null || echo "Database gc_case already exists"
            sudo -u postgres createdb gc_notification 2>/dev/null || echo "Database gc_notification already exists"
            
            # Get DATABASE_URL from .env
            cd ~/genfity/govconnect
            
            # Run migrations for dashboard (has Prisma)
            if [ -d "govconnect-dashboard/prisma" ]; then
              echo "📦 Running Dashboard migrations..."
              
              # Check if dashboard container is running
              if ! sudo docker compose ps dashboard | grep -q "running"; then
                echo "⚠️ Dashboard container not running, skipping migrations"
                exit 0
              fi
              
              # Run migration in a simpler way
              sudo docker exec govconnect-dashboard sh -c "npx prisma migrate deploy || npx prisma db push --accept-data-loss" || echo "⚠️ Migration completed with warnings"
            fi
            
            echo "✅ Database migrations complete!"
          ENDSSH

      - name: Health Check
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
        run: |
          echo "🏥 Running health checks..."
          sleep 30
          
          ssh $VPS_USER@$VPS_HOST << 'ENDSSH'
            echo "Checking services..."
            
            # Check Dashboard
            curl -sf http://localhost:3011/api/health && echo "✅ Dashboard OK" || echo "❌ Dashboard FAILED"
            
            # Check Channel Service
            curl -sf http://localhost:3001/health && echo "✅ Channel OK" || echo "❌ Channel FAILED"
            
            # Check AI Service
            curl -sf http://localhost:3002/health && echo "✅ AI OK" || echo "❌ AI FAILED"
            
            # Check Case Service
            curl -sf http://localhost:3003/health && echo "✅ Case OK" || echo "❌ Case FAILED"
            
            # Check Notification Service
            curl -sf http://localhost:3004/health && echo "✅ Notification OK" || echo "❌ Notification FAILED"
            
            # Show container status and memory usage
            echo ""
            echo "Container Status:"
            sudo docker compose ps
            
            echo ""
            echo "Memory Usage:"
            sudo docker stats --no-stream --format "table {{.Name}}\t{{.MemUsage}}" | grep govconnect
          ENDSSH

      - name: Generate summary
        if: success()
        run: |
          echo "## 🚀 GovConnect Deployment Successful" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Services Deployed:**" >> $GITHUB_STEP_SUMMARY
          echo "- Dashboard: https://govconnect.my.id" >> $GITHUB_STEP_SUMMARY
          echo "- Case Service: https://case.govconnect.my.id" >> $GITHUB_STEP_SUMMARY
          echo "- Channel Service: https://channel.govconnect.my.id" >> $GITHUB_STEP_SUMMARY
          echo "- AI Service: https://ai.govconnect.my.id" >> $GITHUB_STEP_SUMMARY
          echo "- Notification Service: https://notification.govconnect.my.id" >> $GITHUB_STEP_SUMMARY

  # ===================================================================================
  # CLEANUP JOB
  # ===================================================================================
  cleanup:
    name: Cleanup Old Resources
    runs-on: ubuntu-latest
    needs: [deploy]
    if: always()
    
    steps:
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Cleanup server
        continue-on-error: true
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
        run: |
          ssh $VPS_USER@$VPS_HOST << 'ENDSSH'
            echo "🧹 Running scheduled cleanup..."
            sudo docker system prune -f --filter "until=24h"
            echo "✅ Cleanup complete!"
          ENDSSH